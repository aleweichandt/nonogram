// @flow
import { OPTION_VOID as V, OPTION_BLACK as B, type BoardType } from '../game';

// eslint-disable-next-line import/prefer-default-export
export const TEST_BOARD: BoardType = [
  [V, B, V],
  [B, V, B],
  [B, B, B],
];
